--- 
title: Git 學習筆記 - 更新本機資料
description: 如何使用 git pull
pubDatetime: 2026-09-07
tags:
  - Learning
  - Note
  - Git
featured: False
---

常用在遠端 Repo 有更新且操作環境不同的狀況下

可以用`git pull` 來將最新的 Repo 拉到本機 

像是我自己，我會用公司跟家裡的電腦進行更改這個blog的動作

我如果在公司有寫一些，且`git push`到雲端了

那我回到家的時候就要使用`git pull`來更新我家裡電腦的資料

---

當我執行`git clone`時，是可以直接使用`git pull`的

因為`git clone`的時候 Git 已經自動幫你設定好了

但當我今天是用`git init`，就需要多這一步設定

[第一次使用 `git pull`](/blog/posts/git_note/20260802_usage_git4/#5-%E4%B8%8A%E5%82%B3%E5%88%B0%E9%81%A0%E7%AB%AF---git-push)

`git init` -> `git push -u origin main`

`-u` 很重要，就是讓Git記住**本機 main** 對應 **origin 的 main**

因為有設定這個`-u`，所以之後可以直接使用`git pull`

不然可是會每次都要打`git pull origin main`呢

---

### `git pull` 跟 `git pull origin main` 差異

`git pull`：依照目前已經設定好的對應關係，從遠端抓更新回來

`git pull origin main`：從 origin 這個遠端 Repo 的 main 抓更新回來

平時已經設定好對應關係時，就可以用`git pull`，這樣子最方便

如果 Git 還不知道要從哪個遠端、哪個 Branch 抓，或是想明確指定，就可以用：

`git pull origin main`

---

### 本機有修改時，可以直接 `git pull` 嗎？

假設我mian.py在遠端跟在本地都有進行修改過

Git會判斷兩邊的修改是否會有衝突

如果有衝突，Git會要求先處理本機的修改

如果本機和遠端都有新的 Commit，`git pull`需要把兩邊的修改整合

若修改到同一個地方，就有可能發生衝突

那衝突有兩種處理方式：

1. 目前的修改是要保留且留下正式紀錄的

可以 Commit 保留紀錄

```
git add .
git commit -m "修改內容"
git pull
```

2. 如果目前只是做到一半

可以 stash 先暫時存起來

```
git stash
git pull
git stash pop
```

先暫存到 stash -> 再 pull -> 再把 stash 裡面的資料取出

>[!TIP]
>執行`git pull`前，如果本機有尚未 Commit 的修改
>
>建議先 Commit 或使用 stash 暫存，避免遠端更新與本機修改互相衝突

---

### Git Pull 速查表

| 功能 | 指令 | 用途 |
|------|------|------|
| 使用已設定好的遠端關係更新 | `git pull` | 把遠端最新更新拉回目前本機 Branch |
| 明確指定遠端與 Branch | `git pull origin main` | 從 `origin` 的 `main` 拉取更新 |


---
<div style="text-align: center;">

  <a href="/blog/posts/git_note/20260829_usage_git18/"> 上一篇：複製倉庫 </a>
  &nbsp;&nbsp;|&nbsp;&nbsp;
  <a href="/blog/posts/git_note/20260826_usage_git16/"> 下一篇：  </a>

</div>

---

<div style="text-align: center;">
<a href="/blog/posts/git_note/20260730_usage_gitall/"> Git 學習筆記系列總覽 </a>
</div>