--- 
title: Git 學習筆記 - 查看指定 Commit 
description: 如何使用 git show
pubDatetime: 2026-08-10
tags:
  - Learning
  - Note
  - Git
featured: False
---

`git show` -> 用來查看**單一** Commit 的詳細內容

`git log` -> 用來查看**很多** Commit 的詳細內容

---

### 查看目前 Commit 的詳細內容 - `git show`

　　`git show` 預設只顯示一筆 Commit，如果沒指定Commit ID的話，就是目前 HEAD 指向的這筆 Commit

　　其中最重要的是，它**會顯示這筆 Commit 相較於前一筆 Commit 改了什麼**

例如：

```
A → B → C
        ↑
      HEAD (目前的 Commit)
```

　　所以當我執行 `git show` ，等於我是在查看 Commit C 的詳細內容，以及 C 相較於 B 修改了什麼

　　因為是查詢**一筆資料**，所以不用像 log 一樣要按 q 離開

---

### 查看指定 Commit 的詳細內容 - `git show <Commit ID>`

　　先用`git log --oneline`列出簡易歷史，找到想查詢的 Commit ID

　　找到之後執行`git show <Commit ID>` ，就會顯示它相較於前一筆 Commit 修改了哪些內容

---

### 查看 HEAD 前一筆 Commit 的詳細內容 - `git show HEAD~1`

　　例如：

```
A → B → C
        ↑
      HEAD (目前的 Commit)
```

　　執行`git show HEAD~1`，`HEAD~1`代表 HEAD 的前一筆，也就是 Commit B ，一樣會顯示 B 和 A 的比較

　　git show 會顯示「指定 Commit」與「它的上一筆 Commit」之間的差異
---

### 查看目前 Commit 修改了哪些檔案和行數統計 - `git show --stat`

　　`git show --stat`就是查看目前這筆 Commit 修改了哪些檔案，以及新增、刪除了多少行

　　跟`git log --stat`不一樣的地方一樣是 show 只會查看單筆， log 則是多筆

　　也可以查看指定檔案，只要在`--stat`後方加入 Commit ID 就可以了

---

### 查看目前 Commit 修改了哪些檔案 - `git show --name-only`

　　`--name-only` = 只列出檔案名稱

　　所以不會顯示行數統計，只會顯示改了那些檔案的檔案名稱

---

### 查看目前 Commit 修改過哪些檔案，並顯示每個檔案的變動狀態 - `git show --name-status`

　　查詢出來在檔案的前面的英文字母就代表檔案狀態：

　　A = Added 新增檔案

　　M = Modified 修改檔案

　　D = Deleted 刪除檔案

　　R = Renamed 重新命名檔案　

---

### 查看某筆 Commit 中的特定檔案 - git show <Commit ID> -- <檔案路徑>





---

其實我覺得就跟 log 差不多，只是差別在一個顯示單筆，一個顯示多筆

---
<div style="text-align: center;">

  <a href="/blog/posts/20260809_usage_git9/"> 上一篇：git log 指令統整 </a>
  &nbsp;&nbsp;|&nbsp;&nbsp;
  <a href="/blog/posts/20260805_usage_git7/"> 下一篇：  </a>

</div>

---

<div style="text-align: center;">
<a href="/blog/posts/20260730_usage_gitall/"> Git 學習筆記系列總覽 </a>
</div>